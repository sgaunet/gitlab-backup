gitlabProjectID: 456
localpath: /data/gitlab
tmpdir: /tmp
s3cfg:
  endpoint: myendpoint
  bucketName: mybucket
  bucketPath: mybucketpath
  region: myregion
hooks:
  prebackup: "echo prebackup"
  postbackup: "echo postbackup"
noLogTime: false